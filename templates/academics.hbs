{{> header}}
<h2>Course Reflections</h2>
<div class="course-intro">{{{content.main}}}</div>
{{#each courses}}
	<div class="course">
		<h3>{{title}}
			{{#if semester}}
				<span class="semester"> - {{semester}}</span>
			{{/if}}
		</h3>
		{{{main}}}
	</div>
{{/each}}
{{> footer}}